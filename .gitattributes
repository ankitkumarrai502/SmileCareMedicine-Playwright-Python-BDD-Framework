# Normalize line endings: LF in the repo (CI runs on Linux), auto on checkout.
* text=auto eol=lf

# Binary assets — never normalize.
*.xlsx binary
*.png binary
*.ico binary
*.zip binary
*.webm binary
*.mp4 binary
