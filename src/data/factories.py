"""Faker-based data factories for dynamic test data (users, addresses, reviews).

To be implemented later: build valid + boundary inputs, including country-specific phone
numbers that satisfy the AUT's per-country-code regex validation on checkout/contact.
"""

# TODO: implement make_user(), make_address(country_code), make_review(), etc.
